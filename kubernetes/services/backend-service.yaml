apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: hopkinsconnect
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: hopkinsconnect
    component: backend

